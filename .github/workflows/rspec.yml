name: Ruby Gem

on:
  push:
    branches: '**'

jobs:
  build:
    name: RSpec
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 3.1.2
    - run: bundle
    - run: bundle exec rspec
